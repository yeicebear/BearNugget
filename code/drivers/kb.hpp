char kb_getchar();

/*
keyboard driver header file
declares the public interface
kb_getchar returns the next key or zero
this is a minimal header file
no other functions are exported
no structures are exposed
no configuration is needed
shell includes this for input
keeps implementation details hidden
prevents repeated definitions
included with relative path quotes
part of the drivers subsystem
provides single entry point
returns ascii for printable keys
returns special codes for control
*/
// doesnt do much but it is a start 
// i mean its one line but it is a start